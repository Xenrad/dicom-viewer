version: '3.8'

services:
  dicom-viewer:
    build:
      context: .
      dockerfile: Dockerfile.dev
    networks:
      dicom-network:
    environment:
      PORT: ${VIEWER_PORT}

networks:
  dicom-network:
    external: true
